package com.bordershield.officer.data

// Single-checkpoint hackathon build — a real deployment would derive this
// from the officer's assigned post at login instead of a constant.
const val CHECKPOINT = "Attari-Wagah ICP"
